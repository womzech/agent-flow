# AgentFlow AGOS-style registry. Values are placeholders until tooling (e.g.
# ../agent-governance/scripts/agos_doctor.py) updates them. This file is read
# by AGOS scripts; AgentFlow itself does not require it to run.
version: 1
repo: agent-flow
sessions: []
default_agents:
  claude: { installed: unknown }
  codex: { installed: unknown }
  kimi: { installed: unknown }
  copilot: { installed: unknown }
status_files:
  handoff: docs/sales-playbook.md
commands: {}
fanout_backend: none
