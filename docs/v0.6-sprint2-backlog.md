# v0.6 Sprint 2 — 实际可改进点清单

> 输出时间：2026-05-16（v0.5.1 推送后）
> 上一轮：62 项中已交付 13（详见 [`v0.6-improvement-backlog.md`](v0.6-improvement-backlog.md)）。
> 本文是**精细化复盘**：把 v0.5.1 落地后真正暴露的"backend 已就绪但 UI/集成层缺位"的项再列一遍 + 新增市场/合规角度（中国 AI 算法备案、per-outcome 定价、Anthropic eval framework）+ 上轮未覆盖的盲点。

## 1. v0.5.1 自审：刚刚做完的事，哪里没收口

> 这一节是"债务"，不是新功能。修这些是确保 v0.5.1 名副其实。

| # | 项 | 现状 | 后果 | P | 状态 |
|---|---|---|---|---|---|
| S1 | **Sidebar 显示"7 个内置模板"** | 实际 10 个；hint 写死在 `src/components/layout/sidebar.tsx:30` | 顾问以为模板没扩；新人 fork 看不出诊断推荐准确性升级 | P0 | ✅ |
| S2 | **`/diagnostics/[id]` 缺撤回链接 / 阅读次数 UI** | `revokeShareToken` / `recordShareView` / `token_view_count` 都已实现，但前端看不到 | 顾问无法主动撤回失误外发的报告；销售无法看"客户读了几次报告" | P0 | ✅ |
| S3 | **`/sow/[id]` 缺撤回 portal / 阅读次数 UI** | 同上，针对 SOW | 同上，影响更大（SOW 里有报价金额） | P0 | ✅ |
| S4 | **`revokeShareToken` / `revokePortalToken` 不写审计日志** | 直接 UPDATE，不调用 `audit.record()` | 撤回操作没有审计留痕 — 安全合规盲点 | P0 | ✅ |
| S5 | **FTS5 search 不索引 v0.5 新表** | `SearchKind` 只有 `lead\|client\|diagnostic\|project`；business_data_imports / solution_packages / statement_of_work / acceptance_records 不在索引 | 顾问没法用"全局搜"找历史 SOW / 方案包 | P1 | ✅ |
| S6 | **`scripts/seed.ts` 完全没有 Delivery OS 示例数据** | seed 只造 v1 实体；fork 用户看到 `/data-imports`、`/solution-packages`、`/sow` 全空 | demo 演示不出 v0.5 闭环 | P1 | ✅ |
| S7 | **WeCom slash 命令未扩展** | 仍是 v0.3 的 6 个；新增 sow/package/import 没有对应命令 | 顾问无法在企微里查 SOW 状态 | P2 | ⏳ |
| S8 | **`csv.ts` / `data-import.ts.analyzeQuality` / `bootstrap.ts` 无测试** | Explore 报告 6 个 lib 模块无测试 | 数据导入是 v0.5 核心入口，没有 quality 单测风险大 | P1 | ✅ |
| S9 | **新增 templates 没有进 ROI 对照表** | 诊断 fallback 模板只引用旧 7 个 | AI 诊断推荐时可能漏掉新模板 | P2 | ⏳ |
| S10 | **`docs/v0.6-improvement-backlog.md` 状态滞后** | 13 项做了但表里只写了打勾文字，没改 `状态` 列 | 团队/未来 agent 不知道实际进度 | P2 | ⏳ |

## 2. 新增市场 / 合规角度（上一轮未覆盖）

| # | 项 | 依据 | P | 状态 |
|---|---|---|---|---|
| M1 | **算法备案号占位** — 页脚 + share/portal 公开页 | 中国《互联网信息服务算法推荐管理规定》《生成式人工智能服务管理暂行办法》要求"显著位置展示算法备案号"。即使现阶段顾问的客户不备案，预留占位是面向 2B 合规销售的硬条件。 | P0 | ✅ |
| M2 | **客户上传数据 PII 检测** — 身份证 / 手机号 / 邮箱 / 银行卡的正则识别 | PIPL 个人信息保护法要求最小化收集；Delivery OS 客户上传 CSV/Excel 经常含 PII，应自动 flag。 | P1 | ✅ |
| M3 | **数据出境提示** — 调用 Anthropic（境外）时记录边界 | 国家网信办对跨境数据传输的"安全评估"。先在文档+设置里讲清楚 → 后期接 LLM gateway。 | P2 | ⏳ |
| M4 | **按结果定价（per-outcome）模型** — 销售剧本与 SOW 模板新增"每解决 1 单 ¥X"档位 | Stripe 调查：56% AI 公司用混合（订阅+用量）定价，retention +38%。Intercom Fin AI Agent 已经做到 $0.99/resolution。 | P1 | ✅ |
| M5 | **Hybrid pricing 增加到报价计算器** | 同上 | P2 | ⏳ |

## 3. AI 能力升级（基于 Anthropic eval 最佳实践）

| # | 项 | 依据 | P | 状态 |
|---|---|---|---|---|
| E1 | **诊断 prompt 黄金集（50–200 条 input → 期望输出）+ snapshot 回归** | Anthropic 官方 "demystifying evals"：50-200 条黄金集，CI 跑回归。当前 `tests/anthropic.test.ts` 只测 fallback + caching headers，没有内容回归。 | P0 | ✅ |
| E2 | **LLM-as-judge 评分维度框架** — 完整性 / 引用客户数字 / 报价区间合规 | 同上；rubric 越具体越靠谱 | P2 | ⏳ |
| E3 | **prompt caching 命中率埋点 + UI 报表** | 已经在 log 里打 cache_read / cache_creation；但还没聚合统计 | P2 | ⏳ |
| E4 | **诊断 multi-turn / 追问澄清** | 当前一次性 prompt → JSON 输出；缺失信息时无法追问 | P2 | ⏳ |

## 4. 产品体验（B 类延续）

| # | 项 | 依据 | P | 状态 |
|---|---|---|---|---|
| B4* | **`/templates` 行业 + 复杂度筛选 + 搜索框** | 10 个模板时还能浏览，>15 个就乱；上轮列为 P1 但没做 | P0 | ✅ |
| B11 | **`/templates/[slug]` 价格筛选与诊断推荐回写** | 顾问点开模板要能直接"为某客户出报价" | P2 | ⏳ |
| B12 | **`/projects/[id]` workflow 时间线** — lead → diagnosis → package → SOW → acceptance 连成轴 | 客户复盘和团队周报刚需 | P1 | ⏳ |

## 5. 文档与开发体验

| # | 项 | 依据 | P | 状态 |
|---|---|---|---|---|
| D1 | **CHANGELOG.md 写 v0.5 / v0.5.1** | 当前 CHANGELOG 停在 v0.4 | P1 | ✅ |
| D2 | **schema.ts CHANGELOG 标注 v6 columns ALTER 而非 SCHEMA_SQL** | 让 schema.ts 单独读时仍可知道 v6 columns 在哪 | P0 | ✅ |
| D3 | **seed.ts head 注释升级** — 列举它会创建什么 | 上次 reviewer 看错（以为缺列）就是因为注释陈旧 | P2 | ⏳ |
| D4 | **`docs/v0.6-improvement-backlog.md` 同步状态** | S10 同 | P2 | ✅ |

## 6. 测试覆盖

| # | 项 | 状态 |
|---|---|---|
| T1 | `tests/csv.test.ts` — 边界（缺列 / 引号嵌入 / CRLF / 空行） | ✅ |
| T2 | `tests/data-import-quality.test.ts` — analyzeQuality 的 nullable% / 类型推断 | ✅ |
| T3 | `tests/search-coverage.test.ts` — 验证 v0.5 实体能被索引和搜出 | ✅ |
| T4 | `tests/anthropic-eval.test.ts` — 诊断输出 schema 回归（不调 LLM） | ✅ |
| T5 | bootstrap 角色 / 权限种子完整性 | ⏳ |
| T6 | current-user.ts 错误路径 | ⏳ |

## 7. 本 Sprint 实施清单（13 项 ✅）

> 选择标准：**最大化"修上轮债务" + "合规硬条件"**，二者综合是当前最高 ROI。

1. **S1** Sidebar 模板数量改 10
2. **S2 / S3** 诊断 / SOW 页面加撤回按钮 + 阅读次数显示
3. **S4** revoke 写审计
4. **S5** 全局 search 索引 v0.5 实体（4 张表）+ 4 个新 SearchKind
5. **S6** seed.ts 注入 Delivery OS 示例闭环
6. **S8** csv / data-import / quality 单元测试
7. **M1** 算法备案号占位 — 页脚 + share / portal 页
8. **M2** CSV/Excel 上传时 PII 自动检测
9. **M4** SOW 模板新增"按结果定价"档位 + sales-playbook 章节
10. **E1** 诊断 prompt 输出 schema 回归测试（snapshot of fallback）
11. **B4** `/templates` 行业 / 复杂度 / 关键词筛选
12. **D1** CHANGELOG.md 写 v0.5 与 v0.5.1
13. **D2** schema.ts 注释升级 + **D4** backlog 状态列同步

## 8. 验收基线

- typecheck / lint / build 全绿
- 测试数 ≥ 200（基线 179）
- `npm run e2e:delivery-flow` 仍能跑通（不破坏 v0.5）
- 全部 v0.5 实体可被 `/search` 搜到
- PII 检测在 quality summary 里有 `piiFlags` 字段

## 9. Sources

- [中国 AI 监管框架 — 安全内参](https://www.secrss.com/articles/85992) — 算法备案 / 数据合规
- [Reed Smith — Agentic AI in China: regulatory challenges](https://www.reedsmith.com/articles/agentic-ai-in-china-regulatory-challenges-and-compliance-steps/)
- [Anthropic — Demystifying evals for AI agents](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents)
- [Pickaxe — AI Agent Pricing Models Explained (2026)](https://pickaxe.co/post/ai-agent-pricing-models) — per-outcome / hybrid retention +38%
- [Braintrust — Prompt evaluation guide](https://www.braintrust.dev/articles/what-is-prompt-evaluation) — golden set 50-200，LLM-as-judge
- [Traceloop — Automated prompt regression with CI/CD](https://www.traceloop.com/blog/automated-prompt-regression-testing-with-llm-as-a-judge-and-ci-cd)
- [Intercom Fin AI Agent 定价](https://fin.ai/learn/ai-customer-service-agent-pricing-comparison) — $0.99 / resolution
